Walkthrough: Natural Chapter Sorting & Dynamic Test Suite
The media player now correctly handles chapter sorting and dynamically discovers test scripts in the UI.

Key Improvements
1. Robust Natural Chapter Sorting
Chapters are now sorted primarily by their natural numerical title (e.g., 1, 2, 10 instead of 1, 10, 2) and secondarily by their start time.

Chapter List Sorted naturally
Review
Chapter List Sorted naturally

2. Dynamic Test Suite Discovery
The "Tests" tab now automatically scans the 
tests/
 directory and lists all available test scripts, allowing users to run new tests without code changes to the UI.

3. Feature Status & Logbook
A new "✨ Features" button provides a live dashboard and a detailed "Logbuch" for each milestone.

Feature Status Window
Review
Feature Status Window

Chapter Sorting Fix
The sidebar now correctly displays chapters for audiobooks in natural numerical order.

Scrolled View of Chapters
Review
Scrolled View of Chapters

Dynamic Tests
The new "Sorting Advanced" test script was successfully discovered and added to the suite list.

TIP

You can now add any 
.py
 test file to the 
tests/
 directory, and it will automatically appear in the UI for execution.
