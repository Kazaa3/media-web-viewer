hapter Sorting and Media Types
Refining the chapter sorting logic to handle natural numerical order (e.g., 2 before 10) and ensuring robust media categorization (especially M4B as Audiobook).

Proposed Changes
[Component] Backend Parsers & Utils
[MODIFY] 
media_parser.py
Consolidate chapter sorting just before returning: tags['chapters'] = sorted(tags['chapters'], key=lambda x: (x.get('start', 0.0), natural_sort_key(x.get('title', '')))). This ensures consistency regardless of which parser found the chapters.
[MODIFY] 
format_utils.py
Ensure 
natural_sort_key
 handles alphanumeric strings and numeric strings (like "1", "21") by converting all numeric parts to integers for proper comparison.
[MODIFY] 
models.py
Ensure 
.m4b
 files are strictly categorized as "Hörbuch".
Expand categorization logic to support Video, E-Book, and Document types based on extensions.
[Component] Frontend UI
[MODIFY] 
main.py
Add @eel.expose("get_test_suites") to discover 
.py
 files in the 
tests/
 directory.
Update 
run_tests
 to accept filenames directly.
[MODIFY] 
app.html
Update "Tests" tab to dynamically load the checkbox list from 
get_test_suites()
.
Ensure chapter sorting in the sidebar uses the backend-provided order.
[Component] Feature Logbook
Summary of what will change in this component, separated by files.

[NEW] 
logbuch/
Directory containing 
.md
 files for each feature.
[MODIFY] 
main.py
Add @eel.expose("get_logbook_entry") to read files from the logbuch/ directory and return markdown content as HTML (using basic formatting or just text).
[MODIFY] 
app.html
Update the Feature Status modal items to be clickable.
Add a "Logbook" detail modal for displaying the 
.md
 content.
Implement openLogbook(featureName) JavaScript function.
Verification Plan
Automated Tests
Update 
tests/test_types_logic.py
 to include a test for "1, 2, 10, 21, 22" sorting.
Run pytest tests/test_types_logic.py.
PYTHONPATH=. pytest tests/test_sorting_advanced.py
Manual Verification
Load an M4B file with many chapters.
Check the sidebar to ensure chapters are in correct numerical order (1, 2, ..., 10, ..., 21, 22).
Verify metadata (Bitrate, Codec, etc.) is visible.
Open the new "Features" button and verify the list is displayed correctly.
Ensure the modal is responsive and follows the app's design language.

Comment
Ctrl+Alt+M
